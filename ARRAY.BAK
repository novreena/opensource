#include<stdio.h>:
#include<conio.h>
int main()
{
int array[10],maximum,size,a,location=1;
printf("enter the number of elements of an array\n");
scanf("%d",&size);
printf("enter the integer\n",size);
for(a=0;a<size;a++)
scanf("%d",array[a]);
maximum=array[0];
for(a=1;a<size;a++)
{
  if(array[a]>maximum)
  {
   maximum=array[a];
   location=a+1;
   }
  }
  printf("maximun element %d",maximum);
  return 0;
}